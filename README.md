# 兔子镜小剧场 / Rabbit Mirror Theater

v0.31.50：基于 v0.31.49 的交互作用域小修。
- 代码块急救继续保持原始“只拆代码壳、保留主容器 UI”逻辑，不增加新的急救方式。
- 新增独立的交互作用域修复：为每个兔子镜内部的 `id`、`label for`、radio `name` 与 CSS `#id` 引用同步添加唯一前缀，避免不同消息之间互相抢状态。
- 同步处理内部锚点、ARIA 关联与 SVG/CSS `url(#id)` 引用，减少修复交互时破坏图形或局部跳转的风险。
- 增加当前兔子镜内部的 label 点击兜底；不改背景、边框、圆角、布局、普通 class 或现有视觉样式。
- 设置页继续显示“每轮可交互模式（测试版）”。

- 保留 7.10 审美底盘，不沿用 0.31.38～0.31.43 的重度后处理线。
- 删除展现形式池里的「10.1 可交互小剧场」。
- 新增「每轮可交互模式」开关：关闭时不强制内部交互，开启时才每轮强制真实交互。
- 动态渐变模式规则保持原逻辑，不与可交互模式绑定。
- 加回 H.0～H.6 悬疑/怪谈主题组。
- 强化可见中文规则：界面标题、正文、按钮、状态、提示、反馈文案必须使用简体中文。
- 旧 data 属性在急救清洗时会归一为 data-rabbit-mirror。
- 新增主视觉容器完整包裹提示，减少背景空壳、正文掉出主容器的问题。
